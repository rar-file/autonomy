# Test Fixtures

This directory contains sample configuration files used by the test suite.

## Files

- `test-context.json` - Full featured context for integration tests
- `minimal.json` - Minimal context configuration
- `test-config.json` - Test configuration (isolated from production)

## Usage

Tests use these fixtures to avoid modifying production config files.
