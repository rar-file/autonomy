#!/bin/bash
# Onboarding CLI wrapper
# Usage: autonomy onboard {run|quick|reset|status}

AUTONOMY_DIR="${AUTONOMY_DIR:-${OPENCLAW_HOME:-$HOME/.openclaw}/workspace/skills/autonomy}"
bash "$AUTONOMY_DIR/lib/onboard.sh" "$@"
